# Group-4-solavise-final-projet
# Project 4 — Recommendation System

## Objective
Build a recommendation system to suggest **metrics/items** to users.

## Dataset
- `movies.csv` (movieId, title, genres)
- `ratings.csv` (userId, movieId, rating, timestamp)

## Tasks (Required)
1. Explore and preprocess the data (missing values, outliers, etc.)
2. Feature engineering (select relevant features, create new ones if needed)
3. Split data into training and testing sets
4. Train and evaluate collaborative filtering or content-based models
5. Evaluate model performance and create a simple prediction interface

## General Instructions (Checklist)
- GitHub repo + invite members + add facilitators
- Python
- Pandas / NumPy / Scikit-learn / TensorFlow-Keras
  

